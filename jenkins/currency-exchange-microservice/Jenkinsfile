// sample pipeline
pipeline {
    agent [any]
    stages {
        stage ('Build') {
            steps {
				echo "mvn --version"
                sh 'mvn --version'
			}
        }
        stage ('Test') {
            steps {
				echo "run the pipeline"
			}
        }
    }
}
